# opencode (wnxd local fork)

This repo's wnxd-local context lives in `AGENTS.md` (the same file opencode itself reads). See `AGENTS.md` for:

- The local features carried on top of upstream (memory system, `/remember`, neutered compaction, disabled CLAUDE.md auto-loading)
- The upgrade workflow (DO NOT click in-app auto-update — rebuild manually from a merge)
- Build notes and the version-stamping convention

This stub exists only because Claude Code auto-loads `CLAUDE.md` from cwd; the source of truth is `AGENTS.md`.
